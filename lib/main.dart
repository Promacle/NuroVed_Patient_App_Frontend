import 'package:flutter/material.dart';

import 'app/app.dart';

void main() {
  // Ensure Flutter is initialized for native plugins like url_launcher
  WidgetsFlutterBinding.ensureInitialized();

  runApp(const MyApp());
}
